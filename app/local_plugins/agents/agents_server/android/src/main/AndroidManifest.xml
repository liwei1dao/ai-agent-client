<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aiagent.agents_server">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 前台服务保活：进程在锁屏/后台/划掉 app 时继续持有 BLE + AI 对话 + 浮窗 + 音乐 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- 音乐播放子能力（mediaPlayback type） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- 仅浮窗常驻（无麦克风/媒体维度）时的 specialUse 兜底 type -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Android 13+ 常驻通知需要运行时授权；未授权前台服务仍运行，仅通知不展示 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <!-- foregroundServiceType 声明为运行时可能用到的全集；运行时按当前活跃维度
             （agent=microphone+connectedDevice / 音乐=mediaPlayback / 浮窗=specialUse）合并下发。 -->
        <service
            android:name=".AgentsServerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone|connectedDevice|mediaPlayback|specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="assistant_runtime_host" />
        </service>
    </application>
</manifest>
