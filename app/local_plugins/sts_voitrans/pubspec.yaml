name: sts_voitrans
description: VoiTrans end-to-end Speech-to-Speech plugin via WebRTC
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.8.1

flutter:
  plugin:
    platforms:
      android:
        package: com.aiagent.sts_voitrans
        pluginClass: StsVoitransPlugin

dependencies:
  flutter:
    sdk: flutter
  ai_plugin_interface:
    path: ../ai_plugin_interface

dev_dependencies:
  flutter_test:
    sdk: flutter
