# 配置中心服务配置（本地开发）
# 加密密钥不在此处配置，取自环境变量 CONFIG_SECRET_KEY（见 .env.example）。
type: "configcenter"
version: 1.0

sys:
  log:
    FileName: "./log/configcenter.log"
    Loglevel: 5
    MaxAgeTime: 7
    IsDebug: true

  postgres:
    Dsn: "postgresql://postgres.zqwfawlmlxjpgqlyigii:li13451234&&wei@aws-1-us-west-2.pooler.supabase.com:6543/postgres?sslmode=require"

modules:
  configcenter:
    HTTP:
      Addr: ":8091"
    SecretEnv: "CONFIG_SECRET_KEY" # 加密密钥所在的环境变量名
