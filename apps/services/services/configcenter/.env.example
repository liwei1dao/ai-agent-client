# 配置中心服务环境变量（复制为 .env 或在部署环境中导出）
#
# 整包加密密钥：客户端必须用同一字符串解密（见 apps/client/.env 的 CONFIG_SECRET_KEY）。
# 任意长度字符串即可，服务端/客户端都会 SHA-256 派生成 32 字节 AES-256 密钥。
# 未设置时服务会用开发回退密钥并打警告——生产务必配置。
CONFIG_SECRET_KEY=please-change-to-a-long-random-secret
